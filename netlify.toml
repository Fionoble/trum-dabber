[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  COREPACK_ENABLE_STRICT = "0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
